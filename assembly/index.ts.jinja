import { plugin } from '@teraflop/api'

const VERSION = plugin.makeVersion(1, 0, 0)

// The entry point of your plugin.
export function main(): boolean {
  return plugin.register('[[ plugin_name ]]', VERSION)
}

// To help your plugin fail gracefully:
//
// 1. Uncomment the following function, and
// 2. Add `--use abort=assembly/index/abort` argument to the `asc` scripts in your plugin's package.json

// /** Abort this plugin gracefully. */
// function abort(
//   message: string | null,
//   fileName: string | null,
//   lineNumber: u32,
//   columnNumber: u32
// ): void {
//   trace(`[[ plugin_name ]] aborted: ${message}\n\tat ${fileName}:${lineNumber}:${columnNumber}`)
// }
